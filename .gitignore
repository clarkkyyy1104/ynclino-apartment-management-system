## .NET / ASP.NET Core
bin/
obj/
*.user
*.suo
.vs/
*.log

## NuGet
packages/
*.nupkg

## Local SQLite leftovers (no longer used — SQL Server now)
*.db
*.db-shm
*.db-wal

## Environment / secrets
appsettings.*.json
!appsettings.Development.json
!appsettings.json

## Publish output
publish/
*.zip

## OS
.DS_Store
Thumbs.db

# runtime-uploaded images (keep folder placeholders only)
/wwwroot/uploads/lostfound/*
/wwwroot/uploads/maintenance/*
/wwwroot/uploads/profiles/*
!/wwwroot/uploads/lostfound/.gitkeep
!/wwwroot/uploads/maintenance/.gitkeep
!/wwwroot/uploads/profiles/.gitkeep

# PlantUML export output (regenerate from YnclinoClassDiagram.puml)
out/

# per-developer local config (contains DB password) — never commit
appsettings.Local.json
/Database/demo_accounts.local.csv
