@model YnclinoApartmentManagementSystem.Models.ViewModels.LoginViewModel
@{
    Layout = null;
    ViewData["Title"] = "Login";
}
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Login — Apartment Management System</title>
    <link rel="stylesheet" href="~/css/ynclino.css" asp-append-version="true" />
</head>
<body>
    <div>
        <div>
            <h4>Apartment Management System</h4>
            <p>Please sign in to your account</p>
        </div>

        <div>
            <div>
                <form asp-action="Login" method="post">
                    <input type="hidden" name="returnUrl" value="@ViewBag.ReturnUrl" />
                    @Html.AntiForgeryToken()

                    @if (!ViewData.ModelState.IsValid)
                    {
                        <div>
                            <div asp-validation-summary="All"></div>
                        </div>
                    }

                    <div>
                        <label asp-for="Username"></label>
                        <input asp-for="Username" placeholder="Username" autofocus autocomplete="username" />
                    </div>

                    <div>
                        <label asp-for="Password"></label>
                        <div>
                            <input asp-for="Password" id="loginPassword" placeholder="Password" autocomplete="current-password" />
                            <button type="button" onclick="togglePassword('loginPassword', this)" tabindex="-1">Show</button>
                        </div>
                    </div>

                    <button type="submit">Sign In</button>
                </form>

                <hr />

                <div>
                    <form asp-action="QuickLogin" method="post">
                        @Html.AntiForgeryToken()
                        <input type="hidden" name="role" value="Admin" />
                        <button type="submit">Login as Admin</button>
                    </form>
                    <form asp-action="QuickLogin" method="post">
                        @Html.AntiForgeryToken()
                        <input type="hidden" name="role" value="Tenant" />
                        <button type="submit">Login as Tenant</button>
                    </form>
                </div>
            </div>
        </div>
    </div>

    <script>
        function togglePassword(inputId, btn) {
            var input = document.getElementById(inputId);
            if (input.type === 'password') { input.type = 'text'; btn.textContent = 'Hide'; }
            else { input.type = 'password'; btn.textContent = 'Show'; }
        }
    </script>
    <script src="~/js/ynclino.js" asp-append-version="true"></script>
</body>
</html>
